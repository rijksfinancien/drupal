{# based off: drupal/core/modules/system/templates/menu.html.twig #}
<div id="mainMenu">
  <div class="home"><a href="/">
      <span class="home-text">
        <span class="icon-home"></span>Home
      </span>
      <span class="rijks-text">
        Rijksfinanciën.nl
      </span>
    </a></div>

  {% import _self as menus %}
  {{ menus.menu_links(items, attributes, 0) }}
  {% macro menu_links(items, attributes, menu_level) %}
    {% import _self as menus %}
    {% if items %}
      <ul class="mainMenuList">
        {% for item in items %}
          <li{{ item.attributes }}>
            {{ link(item.title, item.url) }}
            {% if item.below %}
              <span class="icon-arrow-down"></span>
              {{ menus.menu_links(item.below, attributes, menu_level + 1) }}
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  {% endmacro %}

  {# Search is dissabled for now #}
  <button class="searchToggle" tabindex="0">
    <span class="icon-search"></span>
    <span class="visually-hidden">{{ 'Search'|t }}</span>
  </button>

  <button class="navbarToggler">
    <span class="text">{{ 'Menu'|t }}</span>
    <span class="bars">
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
      </span>
  </button>
</div>
